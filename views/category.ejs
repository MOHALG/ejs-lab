<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
  <% menuItems.forEach(item => { %>
    <li>
      <strong><%= item.category %></strong><br>
      <%= item.name%>
      <%= item.price%>
      <strong>details:<%= item.details %></strong>
    </li>
  <% }) %>
</ul>
</body>
</html>